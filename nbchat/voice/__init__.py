"""Voice channel (Alfred) — verified voice events, tag parsing, persona.

Every voice utterance is anchored to a verified server-side state
transition or to the model's own output.  No local fakes, no unverified
signals.
"""
